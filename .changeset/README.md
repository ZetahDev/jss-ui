# Changesets

Create a release note and version bump by running:

```bash
pnpm changeset
```

The release workflow on `main` will open a versioning PR and publish packages after the version PR is merged.
