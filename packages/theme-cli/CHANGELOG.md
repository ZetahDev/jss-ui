# @zetahdev/theme-cli

## 0.1.0

### Minor Changes

- f475ae4: Add `jss-theme` CLI with `init` and `generate` commands to create multi-platform theme artifacts (`tokens.css`, `DesignTokens.swift`, and `tokens.xml`) from a single `theme.config.json`.
